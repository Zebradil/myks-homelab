#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
environment:
  baseDomain: ''
  hosts: ['']
  traefik:
    hosts:
      lan: ['']
      vpn: ['']
      web: ['']
  nodes:
    - ''

#! Extra features for the helm plugin
#@overlay/match-child-defaults missing_ok=True
helm:
  #! Remove most of the helm-specific labels and annotations
  removeLabels: true
