#@data/values
---
environment:
  host: junior.zebradil.dev
  id: alpha
  applications:
    - proto: argocd
    #! - proto: cert-manager
    - proto: httpbingo
    #! - proto: webapp
    #!   name: iperf
    #! - proto: webapp
    #!   name: pacoloco
