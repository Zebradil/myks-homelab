#@data/values

#@ load("secrets.star", "sops")

---
application:
  clients:
    - host: #@ sops("dsm-1", "host")
      port: 5001
      https: true
      password: #@ sops("dsm-1", "password")
      username: #@ sops("dsm-1", "username")
