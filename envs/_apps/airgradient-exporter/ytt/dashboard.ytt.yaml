#@ load("@ytt:json", "json")
#@ load("@ytt:data", "data")
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: airgradient-exporter-grafana-dashboard
  labels:
    grafana_dashboard: '1'
data:
  airgradient-exporter-dashboard.json: #@ json.encode(json.decode(data.read("dashboard.json")), indent=2)
