apiVersion: v1
kind: Secret
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: grafana-cloud-endpoints
stringData:
  GC_LOKI_PASSWORD: <path:static/0.sops.yaml#loki | jsonPath {.basicAuth.password}>
  GC_LOKI_URL: <path:static/0.sops.yaml#loki | jsonPath {.host}>
  GC_LOKI_USERNAME: <path:static/0.sops.yaml#loki | jsonPath {.basicAuth.username}>
  GC_PROM_PASSWORD: <path:static/0.sops.yaml#prometheus | jsonPath {.basicAuth.password}>
  GC_PROM_URL: <path:static/0.sops.yaml#prometheus | jsonPath {.host}>
  GC_PROM_USERNAME: <path:static/0.sops.yaml#prometheus | jsonPath {.basicAuth.username}>
  GC_TEMPO_PASSWORD: <path:static/0.sops.yaml#tempo | jsonPath {.basicAuth.password}>
  GC_TEMPO_URL: <path:static/0.sops.yaml#tempo | jsonPath {.host}>
  GC_TEMPO_USERNAME: <path:static/0.sops.yaml#tempo | jsonPath {.basicAuth.username}>
