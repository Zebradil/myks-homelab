apiVersion: v1
kind: Service
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: snmp-exporter
spec:
  ports:
    - name: http
      port: 9116
      targetPort: http
  selector:
    app.kubernetes.io/name: snmp-exporter
