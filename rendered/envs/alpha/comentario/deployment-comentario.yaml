apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: comentario
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: comentario
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        checksum/secrets: 83f4ad6102c4e86b5987e55597d9aa82-1
      labels:
        app.kubernetes.io/name: comentario
    spec:
      containers:
        - args:
            - --verbose
          command:
            - /comentario/comentario
          env:
            - name: HOST
              value: 0.0.0.0
            - name: PORT
              value: "8080"
            - name: SECRETS_FILE
              value: /etc/comentario/secrets.yaml
            - name: BASE_URL
              value: https://comentario.zebradil.dev
          image: registry.gitlab.com/comentario/comentario@sha256:359c8c41f4cda124175a1f7574a3448336b725ea96a96b485e4dbbf7e3b722e8
          name: comentario
          ports:
            - containerPort: 8080
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - all
            privileged: false
            runAsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - mountPath: /etc/comentario/secrets.yaml
              name: secrets
              readOnly: true
              subPath: secrets.yaml
            - mountPath: /data
              name: data
      securityContext:
        fsGroup: 1000
      volumes:
        - name: secrets
          secret:
            secretName: secrets
        - name: data
          persistentVolumeClaim:
            claimName: data
