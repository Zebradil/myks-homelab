apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: pacoloco
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: pacoloco
  template:
    metadata:
      annotations:
        checksum/config: c52e3dd6900ffa9778b633330ec59ed9
      labels:
        app.kubernetes.io/name: pacoloco
    spec:
      containers:
        - image: oci.zebradil.dev/ghcr.io-cache/anatol/pacoloco@sha256:d4c3f8fe457714791af911ae210cc4c9cac3175f95a52f0834703b49ab88cf73
          name: pacoloco
          ports:
            - containerPort: 9129
              name: http
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - all
            privileged: false
          volumeMounts:
            - mountPath: /etc/pacoloco.yaml
              name: config
              readOnly: true
              subPath: pacoloco.yaml
            - mountPath: /var/cache/pacoloco
              name: cache
            - mountPath: /etc/pacman.d/mirrorlist
              name: reflector
              readOnly: true
              subPath: mirrorlist
        - command:
            - /bin/sh
            - -c
            - |
              cp /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist.bak
              sed -i 's/^#Server/Server/' /etc/pacman.d/mirrorlist
              pacoloco -c /etc/pacoloco/pacoloco.yaml
              cp /etc/pacman.d/mirrorlist.bak /etc/pacman.d/mirrorlist
          image: anatol/pacoloco:latest
          name: reflector
          volumeMounts:
            - mountPath: /etc/pacman.d/mirrorlist
              name: reflector
              subPath: mirrorlist
      volumes:
        - configMap:
            name: config
          name: config
        - name: cache
          persistentVolumeClaim:
            claimName: cache
        - emptyDir: {}
          name: reflector
