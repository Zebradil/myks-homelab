apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: cams-cam3-vpn
spec:
  entryPoints:
    - vpn-web
    - vpn-websecure
  routes:
    - kind: Rule
      match: Host(`cams-cam3.ts.zebradil.dev`) || Host(`cams-cam3.gray.ts.zebradil.dev`) || Host(`cams-cam3.junior.ts.zebradil.dev`)
      services:
        - name: cams-cam3
          port: http
