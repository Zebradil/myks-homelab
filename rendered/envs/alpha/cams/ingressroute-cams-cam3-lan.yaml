apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  annotations:
    a8r.io/repository: https://github.com/Zebradil/myks-homelab
  name: cams-cam3-lan
spec:
  entryPoints:
    - lan-web
    - lan-websecure
  routes:
    - kind: Rule
      match: Host(`cams-cam3.lan.zebradil.dev`) || Host(`cams-cam3.gray.lan.zebradil.dev`) || Host(`cams-cam3.junior.lan.zebradil.dev`)
      services:
        - name: cams-cam3
          port: http
