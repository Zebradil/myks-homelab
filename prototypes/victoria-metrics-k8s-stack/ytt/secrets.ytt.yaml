#@ load("@ytt:data", "data")
#@ load("secrets.star", "sops")

#@ a = data.values.application
---
apiVersion: v1
kind: Secret
metadata:
  name: #@ a._.secretName
stringData:
  telegram.botToken: #@ sops("0", "telegram.botToken")
  healthchecksPing.url: #@ sops("0", "healthchecksPing.url")
