#@ load("@ytt:overlay", "overlay")
#@ load("/overlay-matchers.star", m="matchers")

#@overlay/match by=m.kind_name("VMAgent", "vmks")
---
spec:
  remoteWrite:
    #@overlay/append
    - url: http://vmsingle-vmks-longterm.vmks.svc.cluster.local.:8428/api/v1/write
      inlineUrlRelabelConfig:
        - source_labels: ["__name__"]
          regex: "airgradient_.*"
          action: keep
