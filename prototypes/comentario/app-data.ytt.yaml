#@ load("secrets.star", "sops")

#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
application:
  containerPort: 8080
  #! renovate: datasource=docker
  image: registry.gitlab.com/comentario/comentario:v3.14.0-ubuntu
  storageSize: 2Gi
  name: ""
  #! NOTE: The secrets checksum is used to trigger pods restart whin secrets
  #! are updated. YTT doesn't have access to actual secrets values during the
  #! calculation of the checksum, so we use the version of the secrets file to
  #! signify that the secrets have changed.
  secretsVersion: 1
  secrets:
    extensions:
      akismet:
        key: #@ sops("0", "extensions.akismet.key")
    idp:
      github:
        key: #@ sops("0", "idp.github.key")
        secret: #@ sops("0", "idp.github.secret")
      google:
        key: #@ sops("0", "idp.google.key")
        secret: #@ sops("0", "idp.google.secret")
      twitter:
        key: #@ sops("0", "idp.twitter.key")
        secret: #@ sops("0", "idp.twitter.secret")
    smtpServer:
      host: smtp.gmail.com
      port: 587
      username: #@ sops("0", "smtpServer.username")
      password: #@ sops("0", "smtpServer.password")
