#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
application:
  #! Handle application version updates separately to use the same version
  #! in both helm chart and the image puller jobs.
  #! renovate: datasource=github-releases depName=goharbor/harbor
  version: v2.14.2
