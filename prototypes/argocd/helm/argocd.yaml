---
#@ for/end component in ["controller", "redis", "repoServer", "server"]:
#@yaml/text-templated-strings
(@= component @):
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
---
configs:
  cm:
    application.resourceTrackingMethod: annotation
    kustomize.enabled: 'false'
    jsonnet.enabled: 'false'
    helm.enabled: 'false'
