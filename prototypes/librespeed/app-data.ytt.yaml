#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
application:
  #! renovate: datasource=docker
  image: linuxserver/librespeed:5.3.0
  #@schema/validation min_len=1
  password: ""
  #! Example templates from https://github.com/librespeed/speedtest/blob/master/examples/
  #! Uses the default template if empty.
  #@schema/validation one_of=["", "basic", "chart", "customSettings", "gauges", "pretty", "progressBar"]
  template: ""
