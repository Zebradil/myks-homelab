#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
application:
  #! renovate: datasource=docker
  image: filebrowser/filebrowser:v2.28.0
  storage:
    #! Create more entries if you need to mount more volumes and create more PVCs.
    database:
      #! If `existingName` is empty, a new PVC will be created
      existingName: ""
      size: 1Gi
      path: /database
